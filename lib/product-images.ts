/**
 * Product Image Service — PAUSED
 *
 * SerpAPI Key (saved for later): d7afa1ed7baff1687f5c5e2a5f41be0d0c8cce9feaed2025a0ba0cc900c043f0
 *
 * When re-enabling:
 * 1. Set EXPO_PUBLIC_SERPAPI_KEY in .env
 * 2. Uncomment the functions below
 * 3. Create product_images table in Supabase:
 *    create table product_images (
 *      product_id text primary key,
 *      image_url text not null,
 *      search_query text,
 *      updated_at timestamptz default now()
 *    );
 */

// Service paused — SerpAPI free trial exhausted.
// All product cards currently show category icons + Amazon links instead of images.
export {};
